<!DOCTYPE html>
<html>
<head>
  <%- include ("../partials/header.ejs") %>
</head>
  <body>
    <img src="/stylesheets/kot.png" width="100" height="100" class="kotkot" onclick="playSound('/stylesheets/prout.mp3')">

    <h1>Classement</h1>

    <div class="s">
      <a href="s1" class="sl1">S1</a> 
      <a href="/" class="sl2" style="border-bottom: 1px solid rgb(238, 231, 218);">S2</a>
    </div>

    <a href="/stylesheets/tier.png" target="_blank" class="tier">Points par emoji/emote et pourcentage de chances</a>    
    
    <% if(results2.length > 0) { %>
      <% results2.forEach(function(r) { %>
        <p class="time">Dernier prout: <%= r.username %> le <span class=""><%= r.time %></span></p>
      <% }); %>
    <% } else{ %>
      <br><br>
    <% } %>

    <div class="list">
      <ul>
        <% if(results.length > 0) { %> 
          <% results.forEach(function(r) { %>
            <% if(!r.goat) { %>
              <li><span class="username"><%= r.username %></span> - <span class="score"><%= r.score %></span></li>
            <% } else{ %>
              <li> <nobr class="goat">🐐</nobr> <span class="username"><%= r.username %></span> - <span class="score"><%= r.score %></span></li>
            <% } %>
          <% }); %> 
        <% } else{ %>
          <img src="/stylesheets/check.gif">
        <% } %>

      </ul>
    </div>

    <% if(results.length > 3) { %> 
      <img src="/stylesheets/pepePoint.png" class="pepepoint" width="50" height="50">
    <% } else{ %>
      
    <% } %>

    <p class="self">Fait par @03h7</p>
  </body>
</html>

<script>

function playSound(url) {
  new Audio(url).play();
}

</script>